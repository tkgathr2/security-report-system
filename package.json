{
  "name": "security-report-system",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cp -r dist ../backend/frontend-dist && cd ../backend && npm install --include=dev && npm run build:backend",
    "start": "cd backend && npm run migrate:up && npm start"
  }
}
