# Claude Code：Run2 フロント計測前の ingest / debug.log 事実確認 V18.2

これは Claude Code への指示です。

目的：
- フロント再現（V18.0）で `debug.log` が生成/追記されない原因を、推測ではなく**事実確認**で切り分ける。

前提：
- バック(3000)・フロント(5173) は起動中でも良い
- NDJSON ingest は `127.0.0.1:7253` で稼働している想定

禁止：
- シークレットを出力しない（cookie/トークン含む）

---

## V0.1 ingest 疎通（ローカル→ingest）

```powershell
curl.exe -s -S -o NUL -w "%{http_code}" -X POST -H "Content-Type: application/json" -d "{\"test\":\"run2-pre-frontend\"}" "http://127.0.0.1:7253/ingest/f7e4555a-6639-4502-b86d-deba90e7ea4b"
```

期待：`204`

---

## V0.2 debug.log の存在確認（隠し含む）

```powershell
cd C:\Users\takag\00_dev\security-report-system
Get-ChildItem -Force .\.cursor | Format-Table Name,Length,Attributes,LastWriteTime
Test-Path .\.cursor\debug.log
if (Test-Path .\.cursor\debug.log) { Get-Item -Force .\.cursor\debug.log | Format-List FullName,Length,LastWriteTime }
```

期待：
- `debug.log` が存在する（V0.1のPOSTで自動生成される）

もし存在しない場合：
- ingest が別の場所に書いている/失敗している可能性があるので、ここで停止して状況を要約する（推測しない）

---

## V0.3 次のアクション

- 上記がOKなら、Claude For Chrome で `docs/instructions/V18.0_Claude_For_Chrome_Run2_Frontend_Scroll_NoOAuth.md` を実行してフロント操作ログを採取する。

