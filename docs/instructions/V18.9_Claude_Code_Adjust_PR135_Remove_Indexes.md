# Claude Code：PR #135 を「1クエリ化」だけに絞る（インデックスは分離/除外）V18.9

これは Claude Code への指示です。

目的：
- PR #135（`perf/admin-projects-single-query`）に含まれている **インデックス追加migration** を、いったんPRから外す。
- 理由：ローカル実測ではインデックス追加で改善せず、むしろ悪化も観測されたため（推測ではなく実測に基づくリスク低減）。
- PRは「2クエリ→1クエリ化（往復削減）」のみに絞って、レビューしやすく・安全にする。

前提：
- 対象PR：`https://github.com/tkgathr2/security-report-system/pull/135`
- main直push禁止
- シークレットを出力しない

---

## V0.1 ブランチへ移動

```powershell
cd C:\Users\takag\00_dev\security-report-system
git checkout perf/admin-projects-single-query
git status
```

---

## V0.2 migration をPRから外す（削除）

削除対象：
- `backend/migrations/1770700100000_add-indexes-for-admin-projects-list.js`

```powershell
cd C:\Users\takag\00_dev\security-report-system
git rm backend/migrations/1770700100000_add-indexes-for-admin-projects-list.js
git status
```

---

## V0.3 コミット & push（PRを更新）

```powershell
cd C:\Users\takag\00_dev\security-report-system
git commit -m "Chore: Remove index migration from single-query PR"
git push
```

---

## V0.4 完了条件

- PR #135 の差分が `backend/src/routes/admin.ts` のみに絞られていること
- `git status` が clean

補足：
- インデックスは必要性が確定した段階で、別PRとして出す（今は出さない）

