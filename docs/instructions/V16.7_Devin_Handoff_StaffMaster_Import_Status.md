# Devin：スタッフ名マスタ（CSV取込）状況引き継ぎ（事実のみ）

目的：
- Devinが迷子にならないように、確定済みの事実と、次にやるべき最小タスク（Yes/Noで切れる）を整理する。

注意：
- 本書は「事実のみ」。推測や提案は含めない。
- シークレット値（DATABASE_URL等）を本文に書かない。

---

## 1. 本番環境の同定（確定）

- 本番URL：`https://security-report-system-production-926e.up.railway.app/`
- Railway本番プロジェクト：`respectful-embrace`
- Railway本番サービス：`security-report-system`
- 上記URLのドメイン `security-report-system-production-926e.up.railway.app` が Domains に存在することを確認済み

---

## 2. スタッフCSVインポート（確定）

### 2-1. admin_audit_logs（監査ログ）
- `staff_import`：存在（Yes）
- `staff_auto_add`：存在（No）

`staff_import` の内容（要点）：
- created_at：2026-02-02 10:03:02
- admin_email：atsuhiro@takagi.bz
- target_type：staff_master
- payload_json：inserted=29, updated=0, skipped=0, file_name=..._staff_import_utf8.csv

### 2-2. staff_master（件数）
- 総件数：29件
- 当日追加件数：29件（全件 created_at = 2026-02-02 10:03:02）
- created_by：atsuhiro@takagi.bz（全件）

結論：
- 再インポート不要（No）

---

## 3. 文字化け（原因の確定）

事実：
- 変換前の `社長に送るものスタッフ一覧 (3).csv` は UTF-8として読むと文字化けしており、管理画面で「氏名/フリガナ列が見つからない」エラーになる
- 変換後に `氏名,フリガナ` ヘッダーの UTF-8（BOM付き）CSVに整形すると、インポートが成功する

---

## 4. 次の最小タスク（Devinがやる/やらないを決めるための問い）

以下のYes/Noを人間（Cursor）に確認してから着手すること：

1) 「スタッフCSVの取込は“UTF-8以外（CP932等）もアプリ側で自動対応”が必要か？」（Yes/No）
2) 「案件CSVインポート時の staff_auto_add を、本番で意図的に発生させて監査ログを確認する必要があるか？」（Yes/No）

