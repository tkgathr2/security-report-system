# Claude Code：変更をすべてGitHubへアップロード（Devin開発用）V19.6

これは Claude Code への指示です。

目的：
- Devin が続きの開発をできるように、ローカル変更を **すべてGitHubへアップロード**する（ブランチ→PR）。

禁止事項：
- シークレット（DATABASE_URL など）をコミット/出力しない
- main 直push しない（必ずPR）

前提：
- 作業ディレクトリ：`C:\Users\takag\00_dev\security-report-system`

---

## V0.1 現状確認（差分の棚卸し）

```powershell
cd C:\Users\takag\00_dev\security-report-system
git status
git diff --name-only
```

完了条件：
- 変更ファイル一覧が確認できる

---

## V0.2 アップロード用ブランチ作成

```powershell
cd C:\Users\takag\00_dev\security-report-system
git fetch origin
git checkout main
git pull --ff-only origin main
git checkout -b chore/upload-for-devin-v19
```

完了条件：
- `chore/upload-for-devin-v19` にいる

---

## V0.3 変更をコミット（docs系を全部）

方針：
- 今回のGPT側作業は主に `docs/plan.md` と `docs/instructions/V19.*.md` の追加/更新なので、**docs配下はまとめてコミット**する

```powershell
cd C:\Users\takag\00_dev\security-report-system
git add docs
git commit -m "Docs: update plan and add V19 instructions (Devin handoff)"
```

注意：
- `git add docs` の対象に `.env` やシークレットが含まれていないことを `git status` で必ず確認する

完了条件：
- 1コミット作成できる

---

## V0.4 Push & PR作成

```powershell
cd C:\Users\takag\00_dev\security-report-system
git push -u origin chore/upload-for-devin-v19
gh pr create --base main --head chore/upload-for-devin-v19 --title "Docs: plan + V19 instructions (Devin handoff)" --body "Devinが続きの開発に入れるように、plan更新とV19系の指示mdをアップロード。"
```

完了条件：
- PR URL を出力できる

---

## V0.5 PRに含まれる変更の確認（最小）

```powershell
cd C:\Users\takag\00_dev\security-report-system
gh pr view --web
```

完了条件：
- ブラウザでPRを開ける

