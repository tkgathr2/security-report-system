# Claude Code：マージ後に agent log（NDJSON計測コード）を撤去する V19.0

これは Claude Code への指示です。

目的：
- 本件の性能改善が本番で問題なく動くことを確認できた後、`fetch('http://127.0.0.1:7253/ingest/...')` 等の **agent log計測コード**を撤去する。

重要：
- **この手順は「PR #135 がマージ済み」かつ「本番で問題なし確認済み」になってから実行する。**
- 未確認のまま撤去しない（検証手段が消えるため）。

前提：
- main直push禁止
- シークレットを出力しない

---

## V0.1 作業ブランチ作成

```powershell
cd C:\Users\takag\00_dev\security-report-system
git checkout main
git pull --ff-only origin main
git checkout -b chore/remove-agent-logs
```

---

## V0.2 対象箇所を削除（regionごと）

対象（例）：
- `backend/src/routes/admin.ts` の `// #region agent log` 〜 `// #endregion` をすべて削除
- `frontend/src/App.tsx` の `// #region agent log` 〜 `// #endregion` をすべて削除

注意：
- region外の本体ロジック（single-queryのSQL等）は削除しない
- `setTimeout` を「修正」として追加する等はしない

---

## V0.3 型チェック・ビルド

```powershell
cd C:\Users\takag\00_dev\security-report-system\backend
npx tsc --noEmit
cd C:\Users\takag\00_dev\security-report-system\frontend
npx tsc --noEmit
```

---

## V0.4 コミット & PR

```powershell
cd C:\Users\takag\00_dev\security-report-system
git add backend/src/routes/admin.ts frontend/src/App.tsx
git commit -m "Chore: Remove temporary agent instrumentation logs"
git push -u origin chore/remove-agent-logs
gh pr create --base main --head chore/remove-agent-logs --title "Chore: Remove temporary agent logs" --body "検証完了したため一時的なNDJSON計測ログ（agent log）を撤去。"
```

---

## V0.5 完了条件

- PR URL を報告できる
- `git status` が clean

