# Claude Code：リポジトリ同期（最新化）と docs/instructions 変更のPR作成

これは Claude Code への指示です。

目的：
- ローカル作業ツリーを安全に同期（最新化）する
- 今回追加/更新した `docs/instructions/V16.x_*.md` をコミットし、PRを作成する（main直push禁止）

前提：
- 作業ディレクトリ：`C:\Users\takag\00_dev\security-report-system`
- main直pushは禁止。必ずブランチを切ってPRにする。
- シークレット値は表示/貼り付けしない。

---

## V16.8-0 現状確認
以下を実行して結果を日本語で要約してください。

```powershell
cd C:\Users\takag\00_dev\security-report-system
git status
git branch --show-current
git log -5 --oneline
```

---

## V16.8-1 同期（最新化）
未コミット変更がある場合は、まずブランチを作ってその上で作業します。

```powershell
cd C:\Users\takag\00_dev\security-report-system
git fetch origin
git checkout main
git pull --ff-only origin main
```

---

## V16.8-2 ブランチ作成
```powershell
cd C:\Users\takag\00_dev\security-report-system
git checkout -b docs/v16-staff-import-audit
```

---

## V16.8-3 変更のステージング/コミット
対象は今回の調査/手順md（V16.1〜V16.7, V16.8）です。

```powershell
cd C:\Users\takag\00_dev\security-report-system
git add docs/instructions/V16.1_Claude_Code_Staff_CSV_Convert_UTF8.md
git add docs/instructions/V16.2_Claude_For_Chrome_Staff_CSV_Import.md
git add docs/instructions/V16.3_Claude_Code_Check_Staff_Import_Audit.md
git add docs/instructions/V16.4_Claude_For_Chrome_Supabase_SQL_Audit_Check.md
git add docs/instructions/V16.5_Claude_For_Chrome_Railway_SQL_Audit_Check.md
git add docs/instructions/V16.6_Claude_For_Chrome_Railway_Prod_Project_Identify.md
git add docs/instructions/V16.7_Devin_Handoff_StaffMaster_Import_Status.md
git add docs/instructions/V16.8_Claude_Code_Sync_and_PR.md

git status
git commit -m "Docs: Add staff import audit/verification instructions (V16.x)"
```

---

## V16.8-4 Push と PR作成
```powershell
cd C:\Users\takag\00_dev\security-report-system
git push -u origin docs/v16-staff-import-audit
gh pr create --base main --head docs/v16-staff-import-audit --title "Docs: Staff import audit/verification (V16.x)" --body "スタッフCSV取込の調査・検証・監査ログ確定・Devin引き継ぎの手順書（V16.x）を追加。"
```

---

## V16.8-5 完了条件
- PRが作成され、URLを報告できること
- `git status` が clean であること

