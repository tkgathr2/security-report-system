# Claude For Chrome：Devinを最新コミットへ同期（再インデックス）する

これは Claude For Chrome への指示です。

目的：
- Devin側のリポジトリ状態を最新にする（main/PRの最新コミットを取り込ませる）。

注意（SEC-01）：
- この工程ではログイン情報が必要になる可能性があります。自動入力は行いません。SEC-01 で停止します。
- シークレット値をチャットに貼らない。

前提：
- 先にClaude Codeで `docs/instructions/V16.8_Claude_Code_Sync_and_PR.md` を実行し、PRを作成済みであること

---

## V16.9-0 GitHubの状態確認（PRが作成済みか）
1) GitHubで当該PRを開く
2) 変更ファイルに `docs/instructions/V16.1`〜`V16.8` が含まれていることを確認

## V16.9-1 Devinでリポジトリ同期を実行
1) Devinのプロジェクト画面を開く（security-report-system）
2) 画面内の「Sync」「Update」「Re-index」「Refresh」「Pull」等の操作を探す
3) 「mainの最新コミットを取り込む」操作（再インデックス/同期）を実行する

### もし「ブランチ指定で同期」ができる場合
- まず `docs/v16-staff-import-audit` ブランチを同期して、PRの差分が見える状態にする
- 次にPRがmainへマージされたら、mainを再同期する

## V16.9-2 同期結果の報告
このチャットに、次の事実だけを報告してください。
- Devin側の状態表示（例：Indexed / Up to date 等）
- 取り込まれたブランチ名（main か feature か）
- Devinが表示する最新コミットID（短縮でOK）

