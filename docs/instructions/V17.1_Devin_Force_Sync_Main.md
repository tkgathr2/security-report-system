# Devin：origin/mainへ強制同期（36コミットbehind解消）→V16.x存在確認

これは Devin への指示です。

目的：
- Devinのローカルリポジトリが `origin/main` より遅れている（36コミットbehind）ため、`origin/main` に強制的に揃える。
- 揃えた後に、`8f568fb` と `docs/instructions/V16.x` の存在を「ログ」で証明する。

禁止：
- シークレット（DATABASE_URL 等）を表示しない
- 環境変数の実値を出力しない

前提：
- 対象repo：`tkgathr2/security-report-system`

---

## V17.1-0 いま居るディレクトリが正しいか確認

```bash
pwd
ls -la
git rev-parse --is-inside-work-tree
```

期待：
- `git rev-parse --is-inside-work-tree` が `true`

---

## V17.1-1 origin/mainへ強制同期（ローカル変更が無いことが前提）

注意：
- ローカルに未コミット変更がある場合、この操作で消えます。先に `git status` を必ず確認してください。

```bash
git status
git remote -v
git fetch --all --prune
git checkout main
git reset --hard origin/main
```

---

## V17.1-2 最新コミット取り込み確認（必須ログ）

```bash
git rev-parse --abbrev-ref HEAD
git rev-parse HEAD
git log -1 --oneline

# マージコミットが存在するか
git show -s --format=%H 8f568fb
```

期待：
- `git show ... 8f568fb` が成功する（コミットが存在する）

---

## V17.1-3 V16.x ファイルの存在確認（必須ログ）

```bash
ls -la docs
ls -la docs/instructions | sed -n '1,200p'
ls -la docs/instructions | grep -E '^.*V16\.' || true
```

---

## V17.1-4 V16.7 の内容確認（必須ログ）

```bash
sed -n '1,120p' docs/instructions/V16.7_Devin_Handoff_StaffMaster_Import_Status.md
```

---

## V17.1-5 報告（Devin→人間/Cursor）

次をそのまま貼る（推測は書かない）：
- V17.1-1〜V17.1-4 のコマンド出力

