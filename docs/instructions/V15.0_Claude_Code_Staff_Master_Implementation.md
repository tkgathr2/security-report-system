# Claude Code への指示：スタッフ名マスタ機能の実装

## これは Claude Code への指示です。

## 進捗：20%（Planフェーズ：仕様確定→実装開始）

---

## 確定した仕様（Ask完了）

| 項目 | 仕様 |
|------|------|
| 対象 | 既存のスタッフ管理を拡張 |
| CSV列 | 氏名（漢字）、フリガナ |
| 同一人物判定キー | **カナ氏名（姓+名）のみ** |
| 投入経路 | 管理画面のCSVインポート（手動） |
| 自動追加 | 即時反映 |
| 既存更新 | 行う（カナ一致で漢字を上書き） |
| 監査ログ | 記録する |

---

## 実装タスク（順番に実行）

### タスク1: docs/plan.md に「10. スタッフ名マスタ」章を追加

`docs/plan.md` の末尾（9章の後）に以下を追加してください：

```markdown
## 10. スタッフ名マスタ（CSV取込・自動追加）

### 10-1. 目的
- スタッフ名を事前にCSVで登録し、報告書入力時に名前候補から選べるようにする
- 日次の案件CSVに新しい名前が含まれていたら、スタッフマスタへ自動追加する

### 10-2. データ定義
#### staff_master テーブル（既存拡張）
- id uuid PK
- name_kanji text NOT NULL（漢字氏名）
- name_kana text NOT NULL UNIQUE（カナ氏名：同一人物判定キー）
- created_at timestamp NOT NULL
- updated_at timestamp NOT NULL
- created_by text NULL（監査用：追加した管理者メール）

#### CSV列定義
- 氏名（漢字）：必須
- フリガナ（カナ）：必須

### 10-3. API仕様
#### POST /api/admin/staff/import
- 認証：管理者セッション必須
- リクエスト：multipart/form-data（CSVファイル）
- 処理：
  1. CSVをパース（UTF-8前提、BOMあり対応）
  2. 各行について：
     - カナ氏名で既存検索
     - 存在しない → INSERT
     - 存在する → 漢字が異なれば UPDATE
  3. 監査ログ記録（action: staff_import）
- レスポンス：{ inserted: number, updated: number, skipped: number }

### 10-4. 日次データ連携
- 案件CSVインポート（POST /api/admin/csv/import）時に、スタッフ名（キャスト名）を自動でスタッフマスタへ追加
- 同一人物判定はカナ氏名で行う
- 新規のみ追加（既存は更新しない：案件CSV由来の場合）

### 10-5. 監査ログ
- action: staff_import（一括インポート）
- action: staff_auto_add（日次データからの自動追加）
- payload_json: { file_name, inserted, updated, skipped }

### 10-6. 受け入れ条件
- AC-40: スタッフCSV（氏名・フリガナ）をインポートできる
- AC-41: カナ一致で既存スタッフの漢字が更新される
- AC-42: 新規スタッフが即時追加される
- AC-43: インポート操作が監査ログに記録される
- AC-44: 案件CSVインポート時に新しい名前が自動追加される

### 10-7. NG例
- カナ氏名が空欄のまま登録する
- 同一カナで複数レコードを作成する
- 監査ログなしでインポートを実行する
```

---

### タスク2: バックエンド実装（POST /api/admin/staff/import）

`backend/src/routes/staff.ts` に以下を追加（または新規作成）：

```typescript
// POST /api/admin/staff/import - CSVインポート
router.post('/import', requireAdminSession, upload.single('file'), async (req, res) => {
  try {
    const file = req.file;
    if (!file) {
      return res.status(400).json({ error: 'NO_FILE', message: 'ファイルが選択されていません' });
    }

    // CSVパース（UTF-8 BOM対応）
    const csvContent = file.buffer.toString('utf-8').replace(/^\uFEFF/, '');
    const lines = csvContent.split('\n').filter(line => line.trim());
    
    // ヘッダー検証（1行目）
    const header = lines[0].split(',').map(h => h.trim());
    const nameKanjiIdx = header.findIndex(h => h.includes('氏名') || h.includes('漢字'));
    const nameKanaIdx = header.findIndex(h => h.includes('フリガナ') || h.includes('カナ'));
    
    if (nameKanjiIdx === -1 || nameKanaIdx === -1) {
      return res.status(400).json({ 
        error: 'CSV_HEADER_MISMATCH', 
        message: '氏名とフリガナの列が見つかりません' 
      });
    }

    let inserted = 0, updated = 0, skipped = 0;

    // データ行を処理
    for (let i = 1; i < lines.length; i++) {
      const cols = lines[i].split(',').map(c => c.trim().replace(/^"|"$/g, ''));
      const nameKanji = cols[nameKanjiIdx];
      const nameKana = cols[nameKanaIdx];

      if (!nameKana) {
        skipped++;
        continue;
      }

      // カナで既存検索
      const existing = await pool.query(
        'SELECT id, name_kanji FROM staff_master WHERE name_kana = $1',
        [nameKana]
      );

      if (existing.rows.length === 0) {
        // 新規追加
        await pool.query(
          `INSERT INTO staff_master (id, name_kanji, name_kana, created_at, updated_at, created_by)
           VALUES (gen_random_uuid(), $1, $2, NOW(), NOW(), $3)`,
          [nameKanji, nameKana, req.session.adminEmail]
        );
        inserted++;
      } else if (existing.rows[0].name_kanji !== nameKanji) {
        // 漢字更新
        await pool.query(
          'UPDATE staff_master SET name_kanji = $1, updated_at = NOW() WHERE name_kana = $2',
          [nameKanji, nameKana]
        );
        updated++;
      } else {
        skipped++;
      }
    }

    // 監査ログ
    await pool.query(
      `INSERT INTO admin_audit_logs (id, admin_email, action, target_type, payload_json, created_at)
       VALUES (gen_random_uuid(), $1, 'staff_import', 'staff_master', $2, NOW())`,
      [req.session.adminEmail, JSON.stringify({ file_name: file.originalname, inserted, updated, skipped })]
    );

    res.json({ inserted, updated, skipped });
  } catch (err) {
    console.error('Staff import error:', err);
    res.status(500).json({ error: 'INTERNAL_ERROR', message: 'インポートに失敗しました' });
  }
});
```

---

### タスク3: DBマイグレーション（created_by列追加）

`backend/migrations/` に新しいマイグレーションファイルを作成：

```javascript
// 1738500000000_add-created-by-to-staff-master.js
exports.up = async (pgm) => {
  pgm.addColumn('staff_master', {
    created_by: { type: 'text', notNull: false }
  });
};

exports.down = async (pgm) => {
  pgm.dropColumn('staff_master', 'created_by');
};
```

---

### タスク4: フロントエンド実装（CSVインポートUI）

管理画面の「スタッフ管理」ページに「CSVインポート」ボタンを追加：

1. ファイル選択ダイアログ
2. アップロード→結果表示（追加N件、更新N件、スキップN件）

---

### タスク5: 案件CSVインポート時の自動追加

`backend/src/routes/adminCsvImport.ts` の案件インポート処理に、スタッフ名自動追加を組み込む：

- 案件CSV内のキャスト名（カナ）を抽出
- スタッフマスタに存在しなければ INSERT
- action: staff_auto_add で監査ログ記録

---

## 実行手順（Claude Code）

1. `docs/plan.md` に「10. スタッフ名マスタ」章を追加
2. DBマイグレーションファイルを作成
3. `backend/src/routes/staff.ts` にインポートAPIを追加
4. ローカルでビルド確認（npm run build）
5. PR作成（main直push禁止）

---

## 報告

- 追加/変更したファイル一覧
- npm run build の結果
- PR URL

---

## 作成日時

2026-02-02
