# Claude Code への指示：バグ棚卸し（全件回収のための現状把握）＋次の開発タスク提案

## 目的

「バグを全部回収して、開発も進める」ために、まず **現時点のバグ/失敗を漏れなく棚卸し** して、次にやる作業を最小単位に切り出す。

※推測で進めない。必ず実測（ログ/再現/エラー）で確定する。

---

## 前提

- 作業ルート：`C:\Users\takag\00_dev\security-report-system`
- main直push禁止（PR運用）
- シークレット値は出力/転記しない

---

## 手順（そのまま実行）

```
cd C:\Users\takag\00_dev\security-report-system

### 1) 作業ツリーとブランチ状況
git status
git rev-parse --abbrev-ref HEAD
git log --oneline -5

### 2) main 最新化（必要なら）
git fetch origin

### 3) 依存関係の整合（非対話）
cd backend
npm install
cd ..\frontend
npm install
cd ..

### 4) ビルド/型チェック（失敗＝バグとして記録）
cd backend
npm run build
cd ..\frontend
npm run build
cd ..

### 5) ローカル起動（DBなしでも動く範囲でOK）
###    すでに起動中なら再起動しない（重複起動しない）
###    まず health を叩いて生存確認
curl.exe -s http://localhost:3000/health

### 6) 主要な画面/APIのスモーク（手動操作は最小）
###    - 管理画面トップが開けるか（http://localhost:5173）
###    - /api/admin/me が 401/200 どちらか想定内で返るか（認証状態に依存）
curl.exe -s http://localhost:3000/api/admin/me

### 7) 失敗が出たら「バグ」として記録（この場で修正しない）
###    - 失敗コマンド
###    - エラー全文
###    - 再現手順（最小）
###    - 期待結果/実結果
```

---

## 出力（このチャットに報告する内容）

以下のフォーマットで、**バグ候補を最大5件**まで列挙して報告してください（0件でもOK）。

- バグ#1：
  - 発生箇所（画面/API/ビルド）：  
  - 再現手順：  
  - 期待結果/実結果：  
  - エラー全文（省略なし）：  
  - 影響度（高/中/低）：  

（バグ#2以降も同様）

そのうえで「次に着手すべき最小タスク」を **1つ**だけ提案してください（理由1行つき）。

---

## 完了条件

- 現時点の失敗/不具合が「実測ベース」で一覧化され、次の最小タスクが1つに絞れている

---

## 作成日時

2026-02-02

