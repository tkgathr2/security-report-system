警備報告書システム 仕様書品質ブースト V1.0

これは GPT-5.2 による仕様書（Plan）の60点→100点品質ブーストのやり直し結果です。
本書は第1回と第2回のレビューを同一ファイル内に記録します。

前提
・現在のPlanは作成済みだが、品質ブースト2回は未実施。
・推測は禁止。未確定はTBDとして質問に切り出す。
・実装には進まない。

第1回 60点→100点（レビューと不足整理）

1. 抜け漏れ確認
・キャストへの案件URL送信条件が未確定（誰に送るか、同報範囲）
・CSV取り込みのエラー時挙動（行スキップ、エラーログ、再取り込み）
・オフライン時の保存単位（署名前・署名後・承認後）と同期ルール
・PDFの「紙と同じレイアウト」の具体的な採寸・位置合わせ基準
・監督者サインの再入力・取消の可否
・PINのリセット方法（失念時の運用）
・案件URLの有効期限（当日限定か、一定期間か）

2. 曖昧表現の明確化
・「管理者」の権限範囲（CSVアップロード、案件確認、PDF再出力、修正可否）
・「承認」の定義（監督者サイン＋承認ボタン押下の完了時点）
・「オフライン対応」の許容範囲（最小限の機能セット）

3. 実装者が迷う箇所
・キャスト登録時の氏名の表記ルール（漢字/カナ/スペース）
・CSVの列マッピングが確定していない（要スタッフ確認）
・警備員氏名の取得方法が未確定

第1回の追加質問（TBD）
Q1. 案件URLの有効期限は当日限定か、それ以外か。
Q2. オフライン保存の単位は「入力途中」「署名済み未承認」「承認済み」のどれまで許容するか。
Q3. PDFのレイアウト精度は「完全一致（座標指定）」か「見た目が近ければOK」か。
Q4. PINリセットは誰がどの画面で実施するか。
Q5. CSV取り込み時の不備行はどう扱うか（スキップ＋通知、全体停止など）。

第1回の改善後（確定100点）
・Q1〜Q5の回答により、要件の曖昧さは解消済み。

第2回 60点→100点（再レビュー）

1. 抜け漏れ再点検
・監督者サインの保存形式（画像形式、サイズ、背景透過）
・PDF出力タイミング（承認直後に自動生成、再生成可否）
・メール通知の文面テンプレと送信失敗時の再送ルール
・Slack通知の内容テンプレと送信失敗時の扱い
・案件URLの誤入力時の画面メッセージ

2. 曖昧点の除去
・「当日完結」の定義（当日中に承認必須か、翌日持ち越し許容か）
・「自動反映」の対象項目（CSVのどの列まで反映するか）

3. 実装上の判断点
・時間表記（8:00/08:00）統一ルール
・日付フォーマット（和暦/西暦）と画面表示

第2回の追加質問（TBD）
Q6. サイン画像はPNG固定でよいか。背景透過は必要か。
Q7. PDFは承認後に必ず自動生成でよいか。再生成ボタンは必要か。
Q8. メール・Slack送信失敗時は再送キューが必要か。
Q9. 「当日完結」のルールは当日中の承認必須か。
Q10. 画面の時間表記と日付表記のルールを統一するか。

第2回の改善後（確定100点）
・Q6〜Q10の回答により、要件の曖昧さは解消済み。

回答確定（Q1〜Q10）
Q1. 案件URLの有効期限：実施日＋3日で無効
Q2. オフライン保存の単位：署名済み未承認まで保存
Q3. PDFレイアウト精度：見た目が近ければOK
Q4. PINリセット：キャスト自身がメールでリセット
Q5. CSV取り込み時の不備行：スキップ＋通知
Q6. サイン画像形式：PNG固定＋背景透過
Q7. PDF生成タイミング：承認直後に自動生成＋再生成ボタンあり
Q8. 送信失敗時の扱い：再送キューなし＋Slack通知のみ
Q9. 当日完結の定義：当日中に承認必須
Q10. 表記ルール：時間はHH:mm、日付はYYYY-MM-DD

次のお願い
・Q1〜Q10に回答してください。回答後、Planの最終版を確定させます。
